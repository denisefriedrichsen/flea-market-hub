<div class="container">
  <div class="header">
    <h1>Your Profile</h1>
  </div>
  <div class="profile-image">
    <% if @user.photo.attached? %>
      <%= cl_image_tag @user.photo.key %>
    <% else %>
      <%= image_tag image_path("flea-market-2.jpeg")%>
    <% end %>
  </div>
  <div class="profile-information">
    <div class="profile-details">
      <br>
      <br>
      <div class="name"><p><strong>Name:</strong> <%= @user.name %></p></div>
      <div class="email"><p><strong>Email:</strong> <%= @user.email %></p></div>
    </div>
    <div class="profile-buttons-container">
    <div class="profile-buttons">
      <%= link_to "Edit profile", edit_user_registration_path, class: "btn btn-pink" %>
      <%= link_to "Delete profile", user_registration_path, class: "btn btn-pink" %>
    </div>
    </div>
  </div>
</div>
