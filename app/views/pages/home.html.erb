<%= render 'shared/banner' %>

<div class="container mt-2">
    <%= link_to 'View all spots', spots_path, class: "btn btn-primary"%>
</div>

<div class="container mt-2">
  <%= simple_form_for :search, url: root_path, method: :get do |f| %>
    <div class="search-form-control form-group">
      <input class="form-control string required" type="text" name="search[query]" id="search_query" />
      <button name="button" type="submit" class="btn btn-flat">
        <i class="fas fa-search"></i> View all spots
      </button>
    </div>
  <% end %>
</div>

<div class="container">
  <h3> Most popular flea-market spots in Berlin </h3>
  <p> Some Spots coming soon </p>

  <h3> How does it work? </h3>
  <p> Here will be a description of the cool platform we created </p>
</div>
