<div class="banner">
  <div class="container-index">
    <h1>Spots in Berlin</h1>
    <br>
  </div>
</div>

<div class="container">
  <% @spots.each do |spot| %>
    <div class="card-spot">
      <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
      <div class="card-spot-infos">
        <div>
          <h2><%=link_to spot.title, spot_path(spot)%></h2>
          <p><%=link_to spot.description, spot_path(spot)%></p>
        </div>
        <h2 class="card-spot-pricing"><%=link_to spot.price, spot_path(spot)%> €</h2>
        <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-spot-user avatar-bordered" />
      </div>
    </div>
  <% end %>
</div>
